// Public API barrel for fmctl's library surface — the single import surface
// for both external consumers and src/cli (constitution Principle VII, FR-018).
// Populated as modules land; see specs/001-schema-governed-frontmatter/contracts/library-api.md.
export {};
